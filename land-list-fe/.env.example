# Copy to `.env.local` in this directory (Create React App reads it automatically).
# Must point at wherever the Rails API is listening (see land-list-be PORT).

REACT_APP_API_BASE_URL=http://localhost:3001
